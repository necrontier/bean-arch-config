set font Terminus:style=bold:size=10
# Setting the escape key to the Windows key
escape Super_L

bind d exec rofi -show drun
bind c exec st
bind F2 exec firefox
bind F3 exec nemo
bind F4 exec minecraft-launcher
bind R restart
bind Q quit
exec feh --bg-fill /home/chris/Bilder/Wallpapers/0-Wallpaper.jpg
